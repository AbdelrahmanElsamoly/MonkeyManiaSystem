import { MapSelectedChildrenPipe } from './map-selected-children.pipe';

describe('MapSelectedChildrenPipe', () => {
  it('create an instance', () => {
    const pipe = new MapSelectedChildrenPipe();
    expect(pipe).toBeTruthy();
  });
});
